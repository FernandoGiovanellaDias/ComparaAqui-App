import { StyleSheet } from "react-native";
import { fontFamilies, getFontFamily } from "./fontFamilies";

export const utilStyles = StyleSheet.create({
    titulo: {
        textAlign: "center",
        fontSize: 20,
        fontWeight: "bold",
    }

});